# Alfred Plan — Corrections & Open Decisions

_Consistency sweep of the master plan, cross-checked against the real code. Most are naming/consistency fixes I can fold in; **two are genuine design forks that need your call** (money PIN threshold, and Telegram's fate) — see the top of Section C/B and the chat._

**Verified ground truth:** vault = `~/Desktop/SecondBrain`; 139 `@tool` registrations; ~320 test functions; RED_LIST lacks git_branch/commit/diff; memory uses Anthropic Haiku + wiki uses Groq (cloud — to be localized); computer_agent is Sonnet cloud-only.

## ✅ RESOLVED (owner decisions, 2026-06-19)
- **#15 Money PIN threshold —** PIN required on ANY money action ≥ ~$100 (strictest; no tap-only money tier). `MONEY_CONFIRM_THRESHOLD_USD = 100`. Folded into master-plan §11-P2.
- **#11 Telegram —** NOT retired; demoted to the brain-free **dormant fail-safe transport** (kill-switch / sentinel / degraded alerts), since iMessage's chat.db polling needs the macOS Messages stack that'd be down in a crisis. iMessage = everyday channel. Folded into master-plan §Surfaces.

_Remaining items below are naming/consistency fixes + builder-gaps to fold in at build time (no further owner decision needed unless noted)._

All key claims verified. Confirmed findings:
- `git_branch`/`git_commit`/`git_diff` are **NOT** in `RED_LIST` (only `control_screen`, money tools, etc. shown) — the Self-Dev section is correct that this is a hole, but §11-P3 wrongly implies the firewall already covers it.
- `memory/memory.py` uses **Anthropic Haiku** (`claude-haiku-4-5`), `memory/wiki.py` uses **Groq** — the Memory section correctly distinguishes these, but §11 omits both as a build item.
- `control/computer_agent.py` `_call_claude` uses `CLAUDE_MODELS["sonnet"]` (cloud-only) with a CLI `input()` prompt — confirmed exactly as the Domains/Suit section says, but §11-P4 never mentions the local-vision-brain build.
- `security/identity.py` salts (`secrets.token_hex(8)` + sha256) — confirmed.

I have everything. Here is the consistency sweep.

---

# Alfred Master Plan — Consistency Sweep

Scope: full read of `/Users/elnatananbelu/jarvis/docs/superpowers/specs/2026-06-19-alfred-master-plan.md` end to end, cross-checked against the real code. Findings grouped by severity. "§" = the named plan section; "P0–P7" = the Section-11 roadmap phases.

## A. STALE / CONTRADICTORY VAULT-PATH REFERENCES (vault is now `~/Desktop/SecondBrain`)

1. **§Memory, line 924** — Personal Model path written as `_JARVIS/_PersonalModel.md` but prose at line 958/1012 calls the scaffold `_PersonalModel.md` and refers to a `_JARVIS` area inconsistently. *Issue:* the code uses `_JARVIS/_PersonalModel.md` (verified in `memory/vault.py:73`), so line 924 is correct but §4.1/§2.1 drop the `_JARVIS/` prefix. *Fix:* standardize every reference to `_JARVIS/_PersonalModel.md`.

2. **§Memory line 1505 (Inheritance.md path)** — `SecondBrain/_Alfred/Inheritance.md`. *Issue:* the vault's special-notes folder is `_JARVIS/` (code namespace stays `jarvis` per §Architecture §5 locked decision), not `_Alfred/`. A note written to `_Alfred/` will not be picked up by `vault.propose_change`'s `_JARVIS`-area routing (`memory/vault.py:367`). *Fix:* use `_JARVIS/Inheritance.md` (or explicitly decide to add an `_Alfred` area to `vault.py`, but that contradicts "leave the code namespace as jarvis").

3. **CODE (not plan, but the plan must call it out): `control/files.py:17` self-write firewall comment still says `~/Documents/SecondBrain`** as the legitimate-write example. *Issue:* this is the stale Documents stub that was trashed; `memory/vault.py:42` correctly defaults to `~/Desktop/SecondBrain`. The firewall functionally protects the *install tree* (path-based, vault-agnostic) so it is not a security bug, but the comment is misleading and the §Self-Dev / §Resilience sections build on `control/files.py` without flagging it. *Fix:* the plan's Self-Dev §1 and §11-P3 should add a one-line task: "correct the stale `~/Documents/SecondBrain` comment in `control/files.py` to `~/Desktop`."

4. **§Memory line 925** — wiki path `~/Desktop/graphify-out/obsidian/_Memory`. *Issue:* unverified and likely stale — confirm against `memory/wiki.py` actual default before relying on it; the global CLAUDE.md notes graphify is "not currently installed." *Fix:* verify the wiki path in `memory/wiki.py` and align, or mark it as "path TBD, confirm at build."

## B. INTERNAL CONTRADICTIONS BETWEEN SECTIONS

5. **Self-write firewall: NEW module vs. ALREADY EXISTS — direct contradiction.** §Gated-Self-Development §0/§1 and §Security §0 spec a **NEW** `security/self_firewall.py` and say the gate "has no concept of self-targeted writes." But §Architecture §0/line 1745 and §11-P3 (line 1813) state the firewall **already exists** in `control/files.py` (`_INSTALL_ROOT`/`.env`). *Verified:* `control/files.py` exists and enforces an install-tree write guard today; `security/self_firewall.py` and `brain/selfdev.py` do **not** exist. *Issue:* two sections describe the same capability as both built and unbuilt, in two different files, with two different mechanisms (gate-branch classifier vs. independent path guard). *Fix:* reconcile to one design — recommended: "the install-tree guard exists in `control/files.py`; the NEW work is (a) extending it with the FORBIDDEN/SENSITIVE/OPEN classification, (b) consulting it from inside `gate()`, (c) the `brain/selfdev.py` orchestrator." State explicitly that `security/self_firewall.py` either *is* the refactor of `control/files.py` or wraps it — don't imply a greenfield module while the real one sits unmentioned in the same plan.

6. **Self-dev module name is inconsistent across three sections.** §Behavior §5 and §Gated-Self-Dev call it `brain/selfdev.py`; §11-P3 calls it `brain/self_dev.py`. *Verified:* neither exists yet. *Fix:* pick one (`brain/selfdev.py`) and use it everywhere, including the test names (`tests/test_selfdev_pipeline.py` vs `tests/test_self_dev_pipeline.py`).

7. **Test count: 297 vs 320 vs 350.** Project CLAUDE.md says **297**; §Memory/§Domains reference 297; §Architecture §1 says "297 tests"; §11-A (line 1756) and §11 preamble (line 1745) say **350**; the Self-Dev Phase-C example says "all 297 tests pass." *Verified actual:* **320 test functions** across 44 files. *Issue:* no single number is right and they conflict within the plan. *Fix:* replace all hardcoded counts with "the full suite (~320 tests)" or drop the literal and say "all tests green," since the count drifts every build.

8. **Tool count: ~136 vs ~139.** Project CLAUDE.md says ~136; §Domains intro and §Architecture §4 say "~139" / "139." *Verified actual:* **139 `@tool` registrations.** *Fix:* use 139 (or "~140") consistently; correct the project CLAUDE.md's 136.

9. **Memory cloud-provider claim is split and §11 omits it.** §Memory §1 (line 920) correctly says `_compress_history_bg`/`consolidate_facts` call **Anthropic Haiku** and `wiki._extract_and_update_bg` calls **Groq** (verified: `memory/memory.py:172` = `claude-haiku-4-5`, `memory/wiki.py:279` = Groq). *Issue:* §11-P5 (Portable Self) and the whole roadmap **never list "local-ize the memory learning paths"** as a deliverable, yet §Memory §6 makes it build-step #1 and a hard "fully-local" acceptance gate. A builder following only Section 11 would ship Alfred still calling Anthropic/Groq on every fact-consolidation — violating the locked "fully-local, cloud only on opt-in" decision. *Fix:* add "localize memory learning (Haiku/Groq → local Ollama tier)" as an explicit P-level deliverable (belongs in P1 or P2, before any data flows).

10. **`memory/life.py` vs `memory/life_data.py` used interchangeably.** §Domains §4 cites `memory/life.py`; §Memory §1 cites `memory/life_data.py`; §11-P5 self-bundle lists `life.db`. *Verified:* **both files exist** (`memory/life.py` and `memory/life_data.py`) plus `memory/goals.py`. *Issue:* unclear which holds what; a builder will guess. *Fix:* add one line clarifying the split (e.g., "`life_data.py` = table accessors over `life.db`; `life.py` = higher-level life-OS helpers") and reference them consistently.

11. **iMessage "replaces Telegram entirely" vs. Telegram still load-bearing in 4 later sections.** §Surfaces §2 (line 1120) makes a hard decision: "iMessage **replaces Telegram entirely**… `telegram_bot.py` is retired." But §Autonomy (§10 reachability ladder, §11 kill-switch), §Security (§9 dormant fail-safe "reachable through Telegram with the owner allowlist + PIN", line 1358), §Resilience (R1 sentinel "templated Telegram reply," R3 "alerts via Telegram," R5 inheritance "owner-only Telegram commands"), and §11-C all still treat **Telegram as the away-channel/fail-safe**. *Issue:* a flat contradiction — one section retires Telegram, four depend on it (including the most safety-critical dormant fail-safe). *Fix:* decide the resilience channel explicitly. Recommended: "iMessage is the primary away-channel; Telegram is **retained as the brain-free dormant fail-safe transport** (the sentinel and degraded modes use it) because `chat.db` polling needs the full macOS Messages stack, which is exactly what's down in a degraded state." Then update §Surfaces §2 from "retired" to "demoted to fail-safe-only."

12. **`source` band naming: `owner` vs `user`.** §Surfaces (line 1089–1091) introduces `source="owner"`; everywhere else (gate, §Autonomy, §Security, §Self-Dev, code) uses `source="user"`. *Verified:* code uses `user`/`autonomous`/`external`. *Issue:* §Surfaces invents a fourth value that the gate doesn't recognize. *Fix:* use `source="user"` consistently (or, if a rename is intended, spec it as a global migration with the gate change — but that's churn the §Architecture §5 "don't rename" rule argues against).

13. **`actor` band (§Security §5) vs `source` band (everywhere else) — unreconciled gate-signature change.** §Security §5 (line 1283) proposes `gate(tool_name, args, source, actor=..., present=...)`, a new 3-arg identity model. But §Autonomy §1 (line 586) keeps `gate(tool_name, args, agent, risk, source)` and never mentions `actor`. *Issue:* two different proposed gate signatures; the guest-path (`actor=guest`) logic in §Security has no home in the §Autonomy decision-order. *Fix:* merge into one canonical `gate()` signature and one decision order that includes the actor/guest band; cross-reference both sections to it.

## C. LOCKED-DECISION INCONSISTENCIES

14. **Money-confirm `~$100`: scattered constant names.** §Domains/§Autonomy/§11 call it `THRESHOLD`/"~$100"; §Security §3 names it `MONEY_CONFIRM_THRESHOLD_USD` and demands "a single configurable constant… one source of truth." *Issue:* §11-P2 and §Autonomy §6 don't reference that constant name, risking the "scattered magic numbers" outcome §Security explicitly forbids. *Fix:* name `MONEY_CONFIRM_THRESHOLD_USD` (default 100) in every section that mentions the threshold.

15. **Money-confirm tier semantics contradict between §Autonomy and §Security.** §Autonomy §6 (line 671): `amount > THRESHOLD → confirm with gate_level="pin"`. §Security §3 ladder: **T2** = "money ≤ ~$100" (one-tap), **T3** = "money > ~$100" (fresh PIN). These agree. But §11-P2 (line 1801/1805) introduces a **third, higher** threshold: "$120 confirms… **$400 demands PIN**" — implying $100–$400 is a plain confirm and only >$400 is PIN. *Issue:* §11-P2 says PIN kicks in at a *second higher* threshold (~$400), while §Security/§Autonomy say PIN kicks in at the *same* ~$100 line. Direct conflict on when PIN is required. *Fix:* pick one model. Recommended (matches his "heavier gate above a threshold"): ~$100 = one-tap confirm, and a single higher PIN threshold (define `MONEY_PIN_THRESHOLD_USD`) — then make §Security T2/T3 consistent with it (T3 PIN at the *higher* line, not at $100).

16. **Drafts-first "even for present user" vs "present human at home executes red-list."** Locked decision: send-as-him **ALWAYS** drafts-first. §Domains §0 / §Autonomy §1-step-6 keep the existing rule "red-list executes for a present human at home (`source==user and not is_away()`)." §Architecture §2 (line 1612, "P0 — home+auto red-list auto-execution," obs 8111) correctly flags that **send/money must confirm even for the present user**. *Issue:* §Autonomy's decision order (step 6) still grants the present-user red-list bypass, which would let a send fire without a draft — contradicting the locked drafts-first rule and §Architecture's own P0 flag. *Fix:* §Autonomy §1 decision-order must carve send-as-him (and money) **out** of the present-user execute path explicitly — "drafts-first overrides the present-user red-list bypass" — so the locked rule and the P0 fix are encoded in the one place the gate is specified, not just flagged in §Architecture.

17. **Gated self-dev trigger source: `user` (correct) vs the §11 "owner" drift.** All self-dev sections correctly require `source="user"` + trusted + not-external-tainted. §11-P3 (line 1812) says "owner-sourced (`source="user"`)" — fine — but §Surfaces' `source="owner"` (finding 12) leaks the ambiguity into the most security-critical gate. *Fix:* resolved by finding 12; ensure self-dev sections never say `owner`.

18. **Nonstop / deferred-you-gates is honored in §11-C but absent from the per-phase deliverables.** §11-C lists 7 human-in-the-loop items (mic test, enrollment, creds, people data, business descriptions, goals, voice sample). *Issue:* the locked "NONSTOP — defer you-gates, never block" execution model is **not stated as a build invariant** anywhere; individual phases (P1 voice clone, P5 enrollment, P7 creds) read as if they block on the human. There's no spec of *how* each phase defers its you-gate and proceeds with a stub/queue. *Fix:* add a one-paragraph "Deferred you-gate protocol" to §11 (or each phase): every human-dependency is scaffolded with a synthetic fixture + a queued onboarding task, the build proceeds and ships green against the stub, and the real input is dropped in later without a rebuild. This is the locked decision and it's currently only implicit.

## D. GENUINE GAPS A BUILDER WOULD HIT

19. **`git_branch`/`git_commit`/`git_diff` are NOT red-list today, but §11-P3 implies the firewall already covers self-code writes.** *Verified:* `RED_LIST` (`brain/autonomy.py:23`) contains `control_screen`, money tools, sends, file-mutation, `run_shell`, `execute_code`, `git_push` — but **not** `git_branch`/`git_commit`/`git_diff`. §Gated-Self-Dev §6 (line 895) correctly lists "promote git_branch/git_commit/git_diff to risk=red" as a change; §11-P3 omits it and over-claims existing coverage. *Fix:* add the git-tool red-list promotion to §11-P3 deliverables; reconcile with finding 5.

20. **`control/computer_agent.py` is cloud-only (Sonnet) — §11-P4 never specs the local-vision-brain build.** *Verified:* `control/computer_agent.py:122/152` `_call_claude` → `CLAUDE_MODELS["sonnet"]`, and line 94–95 uses a CLI `input()` confirm. §Domains §7 (line 533) correctly specs both fixes (route through `gate()`, build a local vision path behind the cloud opt-in). *Issue:* §11-P4 ("the Suit hardening") lists "every action through gate()" but **omits the fully-local vision brain** — shipping P4 as written leaves the Suit's brain cloud-only, violating "fully-local by default." *Fix:* add "replace `_call_claude` cloud-only path with a local vision model (cloud behind `JARVIS_ALLOW_CLOUD_BRAIN`)" and "replace the CLI `input()` destructive-confirm with the central gate" to §11-P4 deliverables.

21. **Per-domain autonomy mode is required by 3 sections but its migration/`TOOL_DOMAIN` map is unscheduled in Section 11.** §Domains §0, §Autonomy §3 (line 619–621), and §11-P2 (line 1804) all require per-domain `supervised↔auto` + a `TOOL_DOMAIN` registry + a migration. *Issue:* §11-P2's deliverable says "per-domain map persisted" in one line but doesn't mention extending `@tool` with `domain=`, the `TOOL_DOMAIN` resolution, or the `memory/migrations.py` bump — a builder will under-scope it. *Fix:* expand P2 deliverable to name: `@tool(domain=...)` field, `TOOL_DOMAIN` resolver in `gate()`, `autonomy_modes` JSON meta key, and the migration.

22. **The "Suit" red-list extensions (`web_login`, `cancel_subscription`, `submit_assignment`, `book_travel`, `browser_action`/`form_submit`) are specced in §Autonomy §1 but the tools don't exist and §11 never schedules creating them.** *Issue:* §Domains creates `brain/tools/travel.py` (`book_travel`) and a school submission wrapper, but `web_login`, `cancel_subscription`, `form_submit`, `browser_action` are named as red-list entries with no creating module assigned. *Fix:* assign each new red-list tool to a module + phase (most belong in P4 Suit + P7 creds), or mark them as "logical red-list patterns enforced via the computer-use band, not standalone tools" — pick one and say so.

23. **SQLite WAL/busy-timeout (obs 8112) is called a "must-fix-first" blocker in §Autonomy §13 but is absent from Section 11's phases.** §Autonomy §13 (line 767): "Until these are green, no domain graduates past supervised" — listing 8112 (no WAL), 8123/8149 (source downgrade), 8121/8136 (injection), 8145 (dedup). *Issue:* §11 P0–P7 **never schedule** the WAL/lock fix or the source-threading fixes; they're treated as done in §11-A ("two audits closed 32 defects") yet §Autonomy/§Architecture list them as open P0s. *Fix:* either confirm they're actually closed (re-check the obs) or add a "P0.5 — close the must-fix-first gate defects (WAL, source-threading, dedup)" phase before any domain graduation. This is the single biggest scheduling gap.

24. **`brain/session.py` continuity spine (§Surfaces §0) is a hard prerequisite for iMessage + cross-surface continuity but is unscheduled in Section 11.** §Surfaces §0 makes `brain/session.py` + moving `_active_agent` into a `session_state` row the foundation for "one Alfred." *Issue:* no P-phase owns it; §11-P1/P7 touch surfaces without it. A builder shipping P7's "live Gmail/iMessage" would hit per-process session fragmentation. *Fix:* add `brain/session.py` + `session_state` migration as an explicit deliverable (logically P2-adjacent, before iMessage goes live in P7).

25. **`eval/persona_regression.py` is named in 3 sections with overlapping-but-different check lists.** §Persona §5 (drift-gate suite, 5 trait cases), §Memory §4.3 (6-point continuity contract), §Resilience R4 (7-point a–g list including gate/firewall/drafts-first/money). *Issue:* three non-identical "the same Alfred" suites; a builder won't know which is canonical. *Fix:* declare one canonical `eval/persona_regression.py` superset and have each section reference its subset by name, rather than re-listing divergent criteria.

26. **Briefing module name drift.** §Behavior cites `brain/briefing.py` (`generate_briefing`, `build_digest`); §Autonomy cites `brain/runner.py::build_digest`. *Issue:* `build_digest` is attributed to both `briefing.py` and `runner.py`. *Fix:* verify which module owns `build_digest` and standardize (one mention says runner, another briefing).

## E. SMALLER / EDITORIAL

27. **Persona file rename `prompts/personas/jarvis.md → alfred.md`** (§Persona §2.1) contradicts §Architecture §5 "leave the code namespace as jarvis / persona text-edit only." *Issue:* renaming the persona *file* is a path change, not a text edit; §Architecture §5's rebrand table says edit `_LEAN_PERSONA` + greeting strings, not rename files. *Fix:* decide — either rename the file (and update all loaders) or keep `jarvis.md` and edit its contents. The §Architecture "cosmetic, leave namespace" stance argues for keeping the filename.

28. **`prompts/runtime/prompt_loader.py` (§Memory §4.1 pillar 2) and `persona.py` (§11-P1) are named as if they exist; verify.** Neither was confirmed in this sweep. *Fix:* verify these paths or mark as "new."

29. **Draft-from-the-leftover preamble text leaked into the doc.** Lines 22–24, 170–172, 347–349, 776–777, 904, 1074, 1197, 1554, 1746–1748 contain author scaffolding ("I have everything I need… Now I'll write the section," "Here is the section."). *Fix:* strip these — they're not part of the spec and read as unfinished.

30. **`~/.alfred/selfdev-worktrees/` (§Self-Dev Phase A) vs no other `~/.alfred/` usage.** The plan otherwise keeps the `jarvis` namespace and uses repo-relative paths. *Issue:* introduces a new `~/.alfred/` home dir inconsistently. *Fix:* align to the chosen namespace (`~/.jarvis/` if keeping the code namespace, or document `~/.alfred/` as the one deliberate exception).

---

**Highest-priority fixes (block a builder):** #5 + #19 (firewall: built-vs-new + git red-list), #11 (Telegram retired vs. fail-safe dependency), #15 (money PIN threshold: $100 vs $400), #16 (drafts-first vs present-user bypass in the gate spec), #23 (must-fix-first defects unscheduled), #9 + #20 (cloud paths in memory + Suit not scheduled → violates fully-local). Everything else is naming/consistency cleanup.

The grounding files I verified against: `/Users/elnatananbelu/jarvis/control/files.py` (firewall comment still says Documents), `/Users/elnatananbelu/jarvis/memory/vault.py:42` (Desktop default, `_JARVIS/_PersonalModel.md`), `/Users/elnatananbelu/jarvis/brain/autonomy.py:23` (RED_LIST, no git_branch/commit/diff), `/Users/elnatananbelu/jarvis/memory/memory.py:172` (Anthropic Haiku) + `/Users/elnatananbelu/jarvis/memory/wiki.py:279` (Groq), `/Users/elnatananbelu/jarvis/control/computer_agent.py:152` (Sonnet cloud-only + line 95 CLI input), `/Users/elnatananbelu/jarvis/security/identity.py:27` (salted sha256). Actual counts: 139 `@tool`, 320 test functions.